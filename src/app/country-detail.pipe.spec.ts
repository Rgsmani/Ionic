import { CountryDetailPipe } from './country-detail.pipe';

describe('CountryDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
